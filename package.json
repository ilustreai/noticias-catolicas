{
  "name": "noticias-catolicas-automaticas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-html.mjs",
    "generate": "node scripts/generate-daily-selection.mjs",
    "validate": "node scripts/validate-selection.mjs data/daily-selection.json",
    "test": "node --test tests/*.test.mjs"
  }
}
